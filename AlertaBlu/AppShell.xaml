<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="AlertaBlu.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="AlertaBlu"
    FlyoutBehavior="Disabled">

    <!-- Content is assigned in the constructor so the page comes from the DI container. -->
    <ShellContent
        x:Name="HomeContent"
        Title="Início"
        Route="MainPage" />

</Shell>
